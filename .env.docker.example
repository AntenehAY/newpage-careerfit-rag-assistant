# Career Intelligence Assistant - Docker Environment Template
# Copy to .env and fill in your values for containerized deployment
#
# Usage:
#   cp .env.docker.example .env
#   # Edit .env with your API keys and secrets

# -----------------------------------------------------------------------------
# Required: API Keys
# -----------------------------------------------------------------------------

# Anthropic API key for Claude (LLM)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API key for embeddings (text-embedding-3-small)
OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# Storage (paths inside container - use defaults)
# -----------------------------------------------------------------------------

# Vector DB persistence (mounted volume at /app/data)
VECTOR_DB_PATH=/app/data/vectordb

# -----------------------------------------------------------------------------
# Optional: Model Overrides
# -----------------------------------------------------------------------------

# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# EMBEDDING_MODEL=text-embedding-3-small

# -----------------------------------------------------------------------------
# Optional: Logging & Tracing
# -----------------------------------------------------------------------------

# LOG_LEVEL=INFO

# LangSmith for tracing (set to enable)
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_api_key
